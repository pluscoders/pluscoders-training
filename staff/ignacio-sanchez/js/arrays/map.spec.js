describe('map()')

// 1

var spec = 'should concatenate ["a", "b", "c"] and ["d", "e", "f"] into a new array and return ["a", "b", "c","d", "e", "f"]'

var array1 = [2, 4, 8];
var newArray = []

map(array1, function(element) {
    newArray * 2 = value
})

assert(newArray[0] === 4, spec)
assert(newArray[1] === 8, spec)
assert(newArray[2] === 16, spec)

