/**
 * It prints a message the given times in the console.
 *
 * @param {string} message - The message to print.
 * @param {number} times - The times that the message repeats.
 */
  function repeat(string, number) {
    for (var i = 0; i < string, number; i++) {
      console.log('string, number');
    }
  }

  repeat(7);

 //REPASS
//1) variables in javascript
//2) if else in javascript
//3) for loop in javascript
//practice in the console

  
// demo 2

function repeat(str, num) {
  if (num >= 0) {
    return str.repeat(num);
  }
}

console.log(repeat('Manushka❤', 7))
