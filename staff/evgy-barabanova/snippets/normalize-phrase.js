function normalizePhrase(phrase) {
  return phrase[0].toUpperCase() + phase[1].toLowerCase()
}

//OR

var s = 'evgy is the most beatufil person of my world'

s = s[0].toUpperCase() + s.substr(1).toLowerCase();

console.log(s);
