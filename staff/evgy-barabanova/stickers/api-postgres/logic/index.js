const registerUser = require('./registerUser');
const authenticateUser = require('./authenticateUser');
const retrieveUser = require('./retrieveUser');
const stickerUser = require('./stickerUser');

module.exports = {
  registerUser,
  authenticateUser,
  retrieveUser,
  stickerUser,
};
