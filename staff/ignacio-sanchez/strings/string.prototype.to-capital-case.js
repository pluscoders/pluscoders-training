String.prototype.toCapitalCase = function () {
    var stringCapitalCase = '';

    for (var i = 0; i < this.length; i++) {
        curr[i] = this[i]
        var prev = this[i - 1]
    }
    if (
        (curr === ' '
            && curr !== '\t'
            && curr !== '\n'
        )
    )
    stringCapitalCase += this.toUpperCase[i]
}

